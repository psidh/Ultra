// import mongoose, {Schema} from 'mongoose';

// mongoose.connect(process.env.MONGODB_URI)

// mongoose.Promise = global.Promise;

// const FormSchema = new Schema({
//   name: String,
//   email: String,
//   phone: String,
//   interested: String,
//   techStack: String,
//   previousProjects: String,
//   githubLink: String,
// })


// const Form = mongoose.model('Form', FormSchema) || mongoose.model('Form', FormSchema);
// export default Form;
